#include <stdio.h>
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    int i = n;
    while (i >= 1) {
        int j = i;
        while (j >= 1) {
            printf("%d ", j);
            j--;
        }
        printf("\n");
        i--;
    }
    return 0;
}
